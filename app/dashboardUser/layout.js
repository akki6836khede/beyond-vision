export const metadata = {
    title: "User dashboard | Beyond Vision",
    description: "Users of Beyond Vision",
};

export default function UserDashboardLayout({ children }) {
    return <>{children}</>;
}