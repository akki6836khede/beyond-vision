export const metadata = {
    title: "Admin dashboard | Beyond Vision",
    description: "Admins of Beyond Vision",
};

export default function AdminDashboardLayout({ children }) {
    return <>{children}</>;
}