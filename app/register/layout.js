export const metadata = {
    title: "Register user | Beyond Vision",
    description: "User registration of Beyond Vision",
};

export default function RegistrationLayout({ children }) {
    return <>{children}</>;
}